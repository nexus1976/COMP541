version: "3.9"

services:
  deployedmodel_churn:
    image: deployedmodel_churn
    container_name: deployedmodel_churn
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
